1. Định nghĩa hàm 
function functionName(parametnerts) {
    // Khối mã thực thi
}
paramters (tham số) : là biến được liệt kê trong định nghĩa của hàm, dùng để tryển dữ liệu vào hàm 
2. Lời gọi hàm 
functionName( arguments)
arguments ( đối số) là giá trị hoặc biến tuyển truyển vào khi hàm được gọi 
3. Cách sử dụng adEventlistener
element.addEventlistener( " event, function)